{
  "installCommand": "npm install",
  "buildCommand": "npm run build --workspace @waves/frontend",
  "outputDirectory": "apps/frontend/dist",
  "framework": "vite",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
